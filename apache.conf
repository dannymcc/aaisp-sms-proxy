# Log requests without query strings to keep tokens out of access logs
LogFormat "%h %l %u %t \"%m %U %H\" %>s %b \"%{Referer}i\" \"%{User-Agent}i\"" combined_noquerystring
CustomLog /proc/self/fd/1 combined_noquerystring
